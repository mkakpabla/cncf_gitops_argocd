{{- if .Values.front.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: front
  namespace: {{ .Values.namespace }}
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
    cert-manager.io/cluster-issuer: {{ .Values.certManager.clusterIssuer | quote }}
spec:
  tls:
    - hosts:
        - {{ printf "event-map.%s.nip.io" .Values.serverIp }}
      secretName: front-tls
  rules:
    - host: {{ printf "event-map.%s.nip.io" .Values.serverIp }}
      http:
        paths:
          - path: {{ .Values.front.ingress.path }}
            pathType: Prefix
            backend:
              service:
                name: front
                port:
                  number: {{ .Values.front.service.port }}
{{- end }}
